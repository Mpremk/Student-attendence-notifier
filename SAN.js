let isAbsent = true;

   // Check if the student is absent
if (isAbsent) {
  // Display an alert message
  alert("you are marked absent. Please take care of your attendance or else you will shortfall attendance from Giving exam.");
} else {
  // If the student is not absent
  console.log("Student is present. No action required.");
}
